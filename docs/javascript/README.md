# JavaScript

## Algorithm

### Sort

- `TODO` `bubbleSort(numberArray)` 冒泡排序
- `TODO` `bucketSort(numberArray)` 桶排序
- `TODO` `countingSort(numberArray)` 技术排序
- `TODO` `heapSort(numberArray)` 堆排序
- `TODO` `insertionSort(numberArray)` 插入排序
- `TODO` `mergeSort(numberArray)` 归并排序
- `TODO` `quickSort(numberArray)` 快速排序
- `TODO` `radixSort(numberArray)` 基数排序
- `TODO` `selectionSort(numberArray)` 选择排序
- `TODO` `shellSort(numberArray)` 希尔排序

## Array

- `TODO` `Array.from(arrayLike[, mapFn[, thisArg]])` 从类数组对象或者可迭代对象中创建一个新的，浅拷贝的数组实例。
- `TODO` `Array.isArray(obj)` 用来判断某个变量是否是一个数组对象。
- `TODO` `Array.of(element, element, ...[, elementN]]])` 根据一组参数来创建新的数组实例，支持任意的参数数量和类型。
- `TODO` `Array.prototype.copyWithin(target[, start[, end]])` 在数组内部，将一段元素序列拷贝到另一段元素序列上，覆盖原有的值。
- `TODO` `Array.prototype.fill(value[, start[, end]])` 将数组中指定区间的所有元素的值，都替换成某个固定的值。
- `TODO` `Array.prototype.pop()` 删除数组的最后一个元素，并返回这个元素。
- `TODO` `Array.prototype.push(element..., elementN)` 在数组的末尾增加一个或多个元素，并返回数组的新长度。
- `TODO` `Array.prototype.reverse()` 颠倒数组中元素的排列顺序，即原先的第一个变为最后一个，原先的最后一个变为第一个。
- `TODO` `Array.prototype.shift()` 删除数组的第一个元素，并返回这个元素。
- `TODO` `Array.prototype.sort([compareFunction(firstEl,secondEl)])` 对数组元素进行排序，并返回当前数组。
- `TODO` `Array.prototype.splice(start[, deleteCount[, item, item, ...]]]])` 在任意的位置给数组添加或删除任意个元素。
- `TODO` `Array.prototype.unshift(element..., elementN)` 在数组的开头增加一个或多个元素，并返回数组的新长度。
- `TODO` `Array.prototype.concat(value, value, ...[, valueN]]])` 返回一个由当前数组和其它若干个数组或者若干个非数组值组合而成的新数组。
- `TODO` `Array.prototype.includes(valueToFind[, fromIndex])` 判断当前数组是否包含某指定的值，如果是返回 true，否则返回 false。
- `TODO` `Array.prototype.join([separator])` 连接所有数组元素组成一个字符串。
- `TODO` `Array.prototype.slice([begin[, end]])` 抽取当前数组中的一段元素组合成一个新数组。
- `TODO` `Array.prototype.toSource()` 返回一个表示当前数组字面量的字符串。遮蔽了原型链上的 Object.prototype.toSource() 方法。
- `TODO` `Array.prototype.toString()` 返回一个由所有数组元素组合而成的字符串。遮蔽了原型链上的 Object.prototype.toString() 方法。
- `TODO` `Array.prototype.toLocaleString([locales[,options]])` 返回一个由所有数组元素组合而成的本地化后的字符串。遮蔽了原型链上的 Object.prototype.toLocaleString() 方法。
- `TODO` `Array.prototype.indexOf(searchElement[, fromIndex))` 返回数组中第一个与指定值相等的元素的索引，如果找不到这样的元素，则返回 --
- `TODO` `Array.prototype.lastIndexOf(searchElement[, fromIndex = arr.length)` 返回数组中最后一个（从右边数第一个）与指定值相等的元素的索引，如果找不到这样的元素，则返回 --
- `TODO` `Array.prototype.forEach(callback(element[, index[, array]])[, thisArg])` 为数组中的每个元素执行一次回调函数。
- `TODO` `Array.prototype.entries()` 返回一个数组迭代器对象，该迭代器会包含所有数组元素的键值对。
- `TODO` `Array.prototype.every(callback(element[, index[, array]])[, thisArg])` 如果数组中的每个元素都满足测试函数，则返回 true，否则返回 false。
- `TODO` `Array.prototype.some(callback(element[, index[, array]])[, thisArg])` 如果数组中至少有一个元素满足测试函数，则返回 true，否则返回 false。
- `TODO` `Array.prototype.filter(callback(element[, index[, array]])[, thisArg])` 将所有在过滤函数中返回 true 的数组元素放进一个新数组中并返回。
- `TODO` `Array.prototype.find(callback(element[, index[, array]])[, thisArg])` 找到第一个满足测试函数的元素并返回那个元素的值，如果找不到，则返回 undefined。
- `TODO` `Array.prototype.findIndex(callback(element[, index[, array]])[, thisArg])` 找到第一个满足测试函数的元素并返回那个元素的索引，如果找不到，则返回 --
- `TODO` `Array.prototype.keys()` 返回一个数组迭代器对象，该迭代器会包含所有数组元素的键。
- `TODO` `Array.prototype.map(callback(element[, index[, array]])[, thisArg])` 返回一个由回调函数的返回值组成的新数组。
- `TODO` `Array.prototype.reduce(callback(accumulator, currentValue[, index[, array]])[, initialValue])` 从左到右为每个数组元素执行一次回调函数，并把上次回调函数的返回值放在一个暂存器中传给下次回调函数，并返回最后一次回调函数的返回值。
- `TODO` `Array.prototype.reduceRight(callback(accumulator, currentValue[, index[, array]])[, initialValue])` 从右到左为每个数组元素执行一次回调函数，并把上次回调函数的返回值放在一个暂存器中传给下次回调函数，并返回最后一次回调函数的返回值。
- `TODO` `Array.prototype.values()` 返回一个数组迭代器对象，该迭代器会包含所有数组元素的值。
- `TODO` `Array.prototype.copyWithin()`
- `TODO` `Array.prototype.entries()`
- `TODO` `Array.prototype.flat()`
- `TODO` `Array.prototype.flatMap()`

- `TODO` `ArrayFrom()` `Array.from()`
- `TODO` `ArrayIs()` `Array.isArray()`
- `TODO` `ArrayOf()` `Array.of()`
- `TODO` `arrayConcat()` `Array.prototype.concat()`
- `TODO` `arrayEvery()` `Array.prototype.every()`
- `TODO` `arrayFill()` `Array.prototype.fill()`
- `TODO` `arrayFilter()` `Array.prototype.filter()`
- `TODO` `arrayFind()` `Array.prototype.find()`
- `TODO` `arrayFindIndex()` `Array.prototype.findIndex()`
- `TODO` `arrayForEach()` `Array.prototype.forEach(callback(currentValue , index, array), thisArg)`
- `TODO` `arrayIncludes()` `Array.prototype.includes()`
- `TODO` `arrayIndexOf()` `Array.prototype.indexOf()`
- `TODO` `arrayJoin()` `Array.prototype.join()`
- `TODO` `arrayKeys()` `Array.prototype.keys()`
- `TODO` `arrayLastIndexOf` `Array.prototype.lastIndexOf()`
- `TODO` `arrayMap()` `Array.prototype.map()`
- `TODO` `arrayPop()` `Array.prototype.pop()`
- `TODO` `arrayPush()` `Array.prototype.push()`
- `TODO` `arrayReduce()` `Array.prototype.reduce()`
- `TODO` `arrayReduceRight()` `Array.prototype.reduceRight()`
- `TODO` `arrayReverse()` `Array.prototype.reverse()`
- `TODO` `arrayShift()` `Array.prototype.shift()`
- `TODO` `arraySlice()` `Array.prototype.slice()`
- `TODO` `arraySome()` `Array.prototype.some()`
- `TODO` `arraySort()` `Array.prototype.sort()`
- `TODO` `arraySplice()` `Array.prototype.splice()`
- `TODO` `arrayUnshift()` `Array.prototype.unshift()`
- `TODO` `arrayValues` `Array.prototype.values()`
- `TODO` `count(array, val = array )` 统计数组中值出现的次数
- `TODO` `countBy(array, fn)` 根据函数统计数组中值出现的次数
- `TODO` `deepCopyArray` 深拷贝数组
- `TODO` `duplicate(array)` 返回数组中的重复值
- `TODO` `filterFalsy(array)` 过滤虚假值
- `TODO` `flatten(array, depth)` 展平数组到指定深度
- `TODO` `head(array, n)` 返回数组的前 n 个元素
- `TODO` `isArrayEvery(array, fn)` 检测数组的所有元素是否都符合
- `TODO` `isArrayEveryEqual(array)` 检测数组的所有元素是否相等
- `TODO` `isArrayNone(array, fn)` 检测数组的所有元素是否都不符合
- `TODO` `isArraySome(array, fn)` 检测数组的部分元素是否符合
- `TODO` `isArraySorted(array)` 检测数组是否已排序。若为升序，返回 - 降序，返回-- 未排序，返回 -
- `TODO` `offset(array, offset)` 将指定数量的元素移动到数组的末尾。
- `TODO` `rest(array, n)` 返回数组的后 n 个元素
- `TODO` `reverse(array)` 反转数组
- `TODO` `unique(array)` 返回数组中的所有不同值。去重；

## Boolean

- `TODO` `isFalse(boolean)` 检测 false
- `TODO` `isTrue(boolean)` 检测 true

## Date

- `TODO` `date(...args)` 创建 Date 对象
- `TODO` `day(date)` 月份中的第几天
- `TODO` `dayOfWeek` 周中的第几天
- `TODO` `endofDay(date)` 日期的最后一秒。- - -
- `TODO` `firstDayofMonth(date)` 日期所在月的第一天
- `TODO` `format(date, fm)` 格式化日期世界
- `TODO` `hour(date)` 日期的小时
- `TODO` `lastDayofMonth(date)` 日期所在月的最后一天
- `TODO` `lastNDay(date)` 日期前 N 天
- `TODO` `millisecond(date)` 日期的毫秒
- `TODO` `minute(date)` 日期的分钟
- `TODO` `monday(date)` 日期所在周的周一
- `TODO` `month(date)` 日期的月份
- `TODO` `nextDays(date, n)` 指定日期后 N 天后
- `TODO` `nextHours(date, n)` 指定日期的 N 时后
- `TODO` `nextMinutes(date, n)` 指定日期的 N 分后
- `TODO` `nextMonths(date, n)` 指定日期的 N 月后
- `TODO` `nextSeconds(date, n)` 指定日期的 N 秒后
- `TODO` `nextWeeks(date, n)` 指定日期的 N 周后
- `TODO` `nextYears(date, n)` 指定日期的 N 年后
- `TODO` `range` 时间区段
- `TODO` `second(date)` 日期的秒
- `TODO` `startofDay(date)` 日期的第一秒。- - -
- `TODO` `sunday(date)` 日期所在周的周日
- `TODO` `timeline(start, end)` 返回起止时间中单位时间的连续时间数组
- `TODO` `timestamp(date)` 时间戳
- `TODO` `tomorrow(date)` 日期的后一天
- `TODO` `weekofYear(date)` 日期所在年份的周次
- `TODO` `year(date)` 日期的年份
- `TODO` `yesterday(date)` 日期的前一天

## DOM

- `TODO` `isBottomVisible()` 检测页面的底部是否可见
- `TODO` `isRoot()` 检测文档根节点
- `TODO` `isVisibleinViewport(el)` 检测元素在视区中是否可见
- `TODO` `redirect(url)` 重定向到指定 URL
- `TODO` `scrollPosition` 返回当前页的滚动位置
- `TODO` `scrollToTop()` 滚动至页面顶部
- `TODO` `url()` 返回当前 URL

## Function

- `TODO` `apply(thisArg, [argsArray])` `Function.prototype.apply(thisArg, [argsArray])`
- `TODO` `bind(thisArg, ...args)` `Function.prototype.bind(thisArg, ...args)`
- `TODO` `call(thisArg, ...args)` `Function.prototype.call(thisArg, ...args)`

- `TODO` `ajax(option)` 异步 JavaScript 和 XML
- `TODO` `debounce()` 防抖：触发高频事件后 n 秒内函数只会执行一次，如果 n 秒内高频事件再次被触发，则重新计算时间
- `TODO` `forEach(object, callback)` 从左向右遍历对象
- `TODO` `forEachRight(object, callback)` 从右向左遍历对象
- `TODO` `forEachRightArray(object, callback)` 从右向左遍历数组
- `TODO` `query()` 元素选择器
- `TODO` `queryAll()` 元素选择器
- `TODO` `swap(a, b)` 交换两个元素的值
- `TODO` `throttle()` 节流：高频事件触发，但在 n 秒内只会执行一次，所以节流会稀释函数的执行频率
- `TODO` `timeTaken(fn, ...args)` 测量执行函数所用的时间

## Mobile

- `TODO` `mobileType()` 返回手机系统类型。Android，IOS；

## Number

- `TODO` `factorial(number)` 阶乘。所有小于及等于该数的正整数的积，并且 - 的阶乘为 - 自然数 n 的阶乘写作 n!。
- `TODO` `euclideanAlgorithm(a, b)` 欧几里得算法。指用于计算两个非负整数 a，b 的最大公约数
- `TODO` `areaofCircle(radius)` 圆的面积
- `TODO` `avg(...nums)` 平均值
- `TODO` `collatz(number)` Collatz 算法
- `TODO` `digitize(num)` 将数字转换为数字数组
- `TODO` `distance(x1, y1, x2, y2)` 两点间的距离
- `TODO` `gcd(a, b)` 计算两个数字之间最大的公共除数
- `TODO` `isDivisible(dividend, divisor)` 检测第一个数值是否可被另一个数值整除
- `TODO` `isEven(number)` 检测偶数
- `TODO` `isFinite(number)` 检测无穷大
- `TODO` `isInteger(number)` 检测整数
- `TODO` `isLeapYear(number)` 检测闰年
- `TODO` `isNaN(number)` 检测非数值
- `TODO` `isOdd(number)` 检测奇数
- `TODO` `isSafeInteger(number)` 检测安全整数
- `TODO` `LeapYears(startYear, endYear)` 返回范围内的所有闰年
- `TODO` `max(...nums)` 最大值
- `TODO` `min(...nums)` 最小值
- `TODO` `PI()` 圆周率
- `TODO` `randomInteger(min, max = min)` 随机整数
- `TODO` `randomNumber(min, max = min)` 随机数值
- `TODO` `sum(...nums)` 总值
- `TODO` `thousandsSeparator(num)` 千位分隔符

## Object

- `TODO` `extend(target, parent)` 混合属性到目标对象
- `TODO` `isArray(obj)` 检测对象是否为数组
- `TODO` `isBoolean(obj)` 检测对象是否为布尔值
- `TODO` `isBrowser` 检测浏览器环境
- `TODO` `isDate(obj)` 检测对象是否为日期时间
- `TODO` `isDefined(obj)` 检测已定义
- `TODO` `isDOM()` 检测 DOM
- `TODO` `isEmpty(obj)` 检测为空
- `TODO` `isFunction(obj)` 检测函数
- `TODO` `isNull(obj)` 检测 Null
- `TODO` `isNumber(obj)` 检测数值
- `TODO` `isObject(obj)` 检测对象
- `TODO` `isPrimitive(obj)` 检测原始数据类型。字符串、数值、布尔值、Symbol。
- `TODO` `isRegExp(obj)` 检测正则表达式
- `TODO` `isString(obj)` 检测字符串
- `TODO` `isSymbol(obj)` 检测 Symbol
- `TODO` `isUndefined(obj)` 检测 Undefined
- `TODO` `stringify(json)` JSON 转字符串
- `TODO` `typeOf(obj)` 检测对象的数据类型

## RegExp

- `TODO` `regs()` 常用正则表达式
- `TODO` `verifyEmail()` 验证邮箱
- `TODO` `verifyNumber()` 验证数值

## String

- `TODO` `capitalize()` 将字符串的第一个字母大写
- `TODO` `capitalizeEveryWord()` 将字符串中每个单词的首字母大写
- `TODO` `hexToRgba()` hex 十六进制颜色转 rgb 颜色。
- `TODO` `isPalindrome(string)` 检测回文字符串
- `TODO` `isStringOfArray(string)` 检测字符串数组
- `TODO` `isStringOfBoolean(string)` 检测字符串布尔值
- `TODO` `isStringOfNumber(string)` 检测字符串数值
- `TODO` `isValidParenthess()` 检测是否是成对出现的括号。有效的括号；
- `TODO` `longestWord(text)` 返回一段话中最长的单词
- `TODO` `nb()` NB
- `TODO` `randomString(length)array)` 随机字符串
- `TODO` `rate()` 评分字符串星星
- `TODO` `removeDuplicates(string)` 移除字符串中所有相邻重复项
- `TODO` `reverseString(string)` 翻转字符串
- `TODO` `rgbToHex()` rgb 颜色转 hex 十六进制颜色。
- `TODO` `sb()` SB
- `TODO` `toJson(string)` 字符串转 JSON
- `TODO` `urlParams([url = window.location.hr)` 返回当前 URL 中的参数对象
- `TODO` `uuid()` 通用唯一识别码（Universally Unique Identifier）

## Reference

- [30 seconds of code](https://www.30secondsofcode.org/list)
- [Lodash](https://www.lodashjs.com/docs/latest)
- [Underscore](https://underscorejs.org)
- [ChanceJS](https://chancejs.com/)
- [CollectJS](https://collect.js.org)
- [Date-fns](https://date-fns.org/)
- [MathJS](https://mathjs.org/)
- [Moment](http://momentjs.cn/)
